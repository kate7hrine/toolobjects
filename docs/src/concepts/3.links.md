### Links

Links allows to create relation between object instances.

Link can be done with `link()` function. You can also remove the link with `unlink()`, but it's unstable. Before the item saved somewhere, links stores locally in `local_obj.links`. Links can be obtained with `getLinkedItems()` function.

### Dynamic links

...

### Link field insertion

...

### Link order

...
